# Testcontainers overrides the datasource — no DB_URL needed here.
# Flyway runs all migrations against the container on every test run.
spring.jpa.hibernate.ddl-auto=update
spring.flyway.enabled=true

# Disable AI + RAG seeding — no external calls, faster startup
ai.evaluation.api-key=
rag.bootstrap.enabled=false
